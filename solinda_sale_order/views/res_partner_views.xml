<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="customer_management_inherit_res_partner" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.customer.mgt</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Scoring">
                    <group string="Customer Management Scoring">
                        <field name="omset" />
                        <field name="payment_history" />
                        <field name="communication" />
                        <field name="company_image" />
                    </group>
                </page>
            </notebook>
        </field>
    </record>
</odoo>
